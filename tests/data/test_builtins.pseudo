DECLARE a:STRING
DECLARE b:INTEGER
DECLARE c:REAL
DECLARE d:CHAR
DECLARE e:STRING
DECLARE f:STRING
DECLARE g:STRING
DECLARE h:CHAR
DECLARE i:CHAR

a <- "hi there"
b <- 99
c <- 100.1
d <- 'a'
e <- "e"
f <- "10.1"
g <- "15"
h <- '1'
i <- '2'

OUTPUT RIGHT(a, 3)
OUTPUT LENGTH(a)
OUTPUT MID(a, 2,4)
OUTPUT LCASE(d)
OUTPUT UCASE(d)
OUTPUT REAL_TO_INTEGER(c)
OUTPUT INTEGER_TO_REAL(b)
OUTPUT ASC(d)
OUTPUT MOD(b, 3)
OUTPUT INTEGER_TO_STRING(b)
OUTPUT REAL_TO_STRING(c)
OUTPUT CHAR_TO_STRING(d)
OUTPUT STRING_TO_CHAR(e)
OUTPUT STRING_TO_REAL(f)
OUTPUT STRING_TO_INTEGER(g)
OUTPUT CHAR_TO_REAL(h)
OUTPUT CHAR_TO_INTEGER(i)
